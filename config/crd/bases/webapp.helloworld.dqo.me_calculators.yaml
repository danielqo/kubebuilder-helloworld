---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: calculators.webapp.helloworld.dqo.me
spec:
  group: webapp.helloworld.dqo.me
  names:
    kind: Calculator
    listKind: CalculatorList
    plural: calculators
    singular: calculator
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Total Value
      jsonPath: .spec.values
      name: Values
      type: string
    - description: Total Value
      jsonPath: .status.totalValue
      name: Total
      type: integer
    name: v1
    schema:
      openAPIV3Schema:
        description: Calculator is the Schema for the calculators API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CalculatorSpec defines the desired state of Calculator
            properties:
              values:
                description: |-
                  ValueA int32 `json:"valueA,omitempty"`
                  ValueB int32 `json:"valueB,omitempty"`
                items:
                  format: int32
                  type: integer
                type: array
            type: object
          status:
            description: CalculatorStatus defines the observed state of Calculator
            properties:
              totalValue:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                format: int32
                type: integer
            required:
            - totalValue
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
